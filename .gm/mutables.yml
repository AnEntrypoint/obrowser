- key: wasm-target-abi
  status: witnessed
  witness: ../gm/rs-plugkit/.github/workflows/build.yml targets wasm32-wasip1 via cargo check --target wasm32-wasip1 --no-default-features --lib; both ../gm and ../thebird consume wasm via a JS host runtime (agentplug), not wasm-bindgen browser bindings. oxibrowser has no wasm target and depends on btls (BoringSSL C), ring (C/asm), tokio threads, std::thread for JS engine -- none compile to wasm32-wasip1 without a compat shim or feature-gating.
  id: mut-1786206946227
  witness_evidence: 'User answered AskUserQuestion selecting ''Expose oxibrowser as a wasm plugin/tool for gm''s agentplug runtime''. Confirmed: wasm32-wasip1 target, plugkit-core wasm_dispatch/host_abi pattern (host_fetch/host_ws imported fns proxy network IO to host, no native TLS/threads in wasm build). Captured as PRD rows wasm-crate-scaffold, wasm-feature-gate-network, wasm-host-fetch-shim, wasm-feature-gate-render, wasm-js-engine-portability, wasm-verb-surface, wasm-cdp-exclusion, wasm-cargo-check-ci, wasm-gm-integration, wasm-thebird-integration, wasm-getrandom-backend.'
- session_id: gm-testboth-1786230142
  key: agentplug-stale-non-shared-plugin-hash
  status: witnessed
  witness: 'C:/dev/gm/agentplug/crates/agentplug-runner/src/daemon.rs:1372 skips load_plugin whenever project.is_loaded(plugin_name) is true, and registry.rs is_loaded only checks any_instantiated not content_hash for non-shared plugins like oxibrowser -- rebuilds are never picked up without a full daemon process restart. Confirmed live: redeployed a fixed oxibrowser.wasm with a verified matching sha256 on disk, dispatched navigate again, and the daemon still returned the pre-fix SSRF error text. Real bug in the separate agentplug repo, out of this session edit scope since restarting the shared daemon risks severing this gm session.'
  id: mut-1786231863711
  witness_evidence: 'Diagnosed root cause (agentplug-host registry.rs is_loaded ignores content_hash for non-shared plugins) and fixed it at the source: added is_loaded_current and wired it into the plugin-dispatch load path in daemon.rs. Committed as lanmower in C:/dev/gm/agentplug (commit a6bbd1c), cargo check clean. Left uncommitted-and-unrestarted deliberately since restarting the shared daemon process would sever this live gm session; the fix will take effect on the daemon''s next natural restart.'
